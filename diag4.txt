=== markOrderFulfilled ===
--- occurrence 1 ---
' class="order-total">${money(o.total)}</div><button class="import-btn" onclick="markOrderFulfilled(\'${o.id}\')">✓ Remise au client</button></div></div>`).join(\'\');\n}\n/* Marque la commande comme remise — ne touche PAS au stock ni ne crée de nouvelle vente (déjà fait au moment de la commande en ligne) */\nasync function markOrderFulfilled(orderId){\n  const order = CACHED_PENDING_ORDERS.find(o=>o.id===orderId); if(!order) return;\n  if(!confirm(`Confirmer la remise de la commande de ${order.client_name} (${money(order.total)}) ?`)) return;\n  const {error} = await sb.from(\'sales\').update({fulfilled:true}).eq(\'id\', orderId);\n  if(error){ console.error(error); alert("Erreur."); return; }\n  loadPendingOrders();\n}\n\nfunction renderCaisseProductGrid(){\n  const grid=document.getEleme'
--- occurrence 2 ---
' nouvelle vente (déjà fait au moment de la commande en ligne) */\nasync function markOrderFulfilled(orderId){\n  const order = CACHED_PENDING_ORDERS.find(o=>o.id===orderId); if(!order) return;\n  if(!confirm(`Confirmer la remise de la commande de ${order.client_name} (${money(order.total)}) ?`)) return;\n  const {error} = await sb.from(\'sales\').update({fulfilled:true}).eq(\'id\', orderId);\n  if(error){ console.error(error); alert("Erreur."); return; }\n  loadPendingOrders();\n}\n\nfunction renderCaisseProductGrid(){\n  const grid=document.getElementById(\'caisseProductGrid\'); if(!grid) return;\n  grid.innerHTML = PRODUCTS.filter(p=>p.active && stockFor(caisseSelectedPoint,p.id)>0).map(p=>{ const qty=stockFor(caisseSelectedPoint,p.id);\n    return `<div class="menu-card ${p.cat}" oncl'

=== sidebar ===
--- occurrence 1 ---
'e .admin-body{flex:1;overflow-y:auto;max-height:calc(100vh - 120px);}\n\n  .admin-sidebar{position:fixed;left:0;top:0;bottom:0;width:260px;background:linear-gradient(180deg,var(--coffee-black) 0%,var(--coffee-brown) 100%);z-index:201;transform:translateX(-100%);transition:transform .3s ease;padding:80px 20px 20px;box-shadow:4px 0 12px rgba(0,0,0,0.3);}\n  .admin-sidebar.open{trans'
--- occurrence 2 ---
'3s ease;padding:80px 20px 20px;box-shadow:4px 0 12px rgba(0,0,0,0.3);}\n  .admin-sidebar.open{transform:translateX(0);}\n  .admin-sidebar-toggle{position:fixed;left:16px;top:16px;z-index:202;width:44px;height:44px;border-radius:12px;background:var(--coffee-brown);color:var(--white);display:flex;align-items:center;justify-content:center;font-size:20px;border:1px solid rgba(221,165'
--- occurrence 3 ---
'12px rgba(0,0,0,0.3);}\n  .admin-sidebar.open{transform:translateX(0);}\n  .admin-sidebar-toggle{position:fixed;left:16px;top:16px;z-index:202;width:44px;height:44px;border-radius:12px;background:var(--coffee-brown);color:var(--white);display:flex;align-items:center;justify-content:center;font-size:20px;border:1px solid rgba(221,165,63,0.3);}\n  .admin-sidebar-toggle:hover{backgro'
--- occurrence 4 ---
'-content:center;font-size:20px;border:1px solid rgba(221,165,63,0.3);}\n  .admin-sidebar-toggle:hover{background:var(--coffee-black);transform:scale(1.05);}\n  .admin-sidebar-menu{list-style:none;padding:0;margin:0;}\n  .admin-sidebar-menu li{margin-bottom:8px;}\n  .admin-sidebar-menu button{width:100%;padding:12px 16px;background:rgba(255,255,255,0.08);color:var(--white);border:1p'
--- occurrence 5 ---
'ar-toggle:hover{background:var(--coffee-black);transform:scale(1.05);}\n  .admin-sidebar-menu{list-style:none;padding:0;margin:0;}\n  .admin-sidebar-menu li{margin-bottom:8px;}\n  .admin-sidebar-menu button{width:100%;padding:12px 16px;background:rgba(255,255,255,0.08);color:var(--white);border:1px solid rgba(221,165,63,0.2);border-radius:10px;font-size:14px;font-weight:600;text-a'
--- occurrence 6 ---
'ale(1.05);}\n  .admin-sidebar-menu{list-style:none;padding:0;margin:0;}\n  .admin-sidebar-menu li{margin-bottom:8px;}\n  .admin-sidebar-menu button{width:100%;padding:12px 16px;background:rgba(255,255,255,0.08);color:var(--white);border:1px solid rgba(221,165,63,0.2);border-radius:10px;font-size:14px;font-weight:600;text-align:left;display:flex;align-items:center;gap:10px;}\n  .adm'
--- occurrence 7 ---
'none;padding:0;margin:0;}\n  .admin-sidebar-menu li{margin-bottom:8px;}\n  .admin-sidebar-menu button{width:100%;padding:12px 16px;background:rgba(255,255,255,0.08);color:var(--white);border:1px solid rgba(221,165,63,0.2);border-radius:10px;font-size:14px;font-weight:600;text-align:left;display:flex;align-items:center;gap:10px;}\n  .admin-sidebar-menu button:hover{background:rgba('
--- occurrence 8 ---
'-weight:600;text-align:left;display:flex;align-items:center;gap:10px;}\n  .admin-sidebar-menu button:hover{background:rgba(255,255,255,0.15);border-color:var(--gold);}\n  .admin-sidebar-menu button.active{background:var(--rust);border-color:var(--rust);color:var(--white);}\n  .admin-sidebar-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:200;display:none;opacity:'
--- occurrence 9 ---
'ton:hover{background:rgba(255,255,255,0.15);border-color:var(--gold);}\n  .admin-sidebar-menu button.active{background:var(--rust);border-color:var(--rust);color:var(--white);}\n  .admin-sidebar-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:200;display:none;opacity:0;transition:opacity .3s ease;}\n  .admin-sidebar-overlay.open{display:block;opacity:1;}\n\n  /* --'
--- occurrence 10 ---
'e{background:var(--rust);border-color:var(--rust);color:var(--white);}\n  .admin-sidebar-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:200;display:none;opacity:0;transition:opacity .3s ease;}\n  .admin-sidebar-overlay.open{display:block;opacity:1;}\n\n  /* ---------- CAISSE (POS) ---------- */\n  .caisse-layout{display:flex;height:100%;gap:0;position:relative;}\n '
--- occurrence 11 ---
',0.5);z-index:200;display:none;opacity:0;transition:opacity .3s ease;}\n  .admin-sidebar-overlay.open{display:block;opacity:1;}\n\n  /* ---------- CAISSE (POS) ---------- */\n  .caisse-layout{display:flex;height:100%;gap:0;position:relative;}\n  .caisse-left{flex:1;overflow-y:auto;padding-right:24px;min-height:0;}\n  .caisse-right{width:380px;flex-shrink:0;position:sticky;top:0;align'
--- occurrence 12 ---
"ist.toggle('active', t.dataset.tab===tab));\n  document.querySelectorAll('.admin-sidebar-menu button').forEach(b=>b.classList.toggle('active', b.dataset.tab===tab));\n  if(tab==='produits') renderAdminProducts();\n  if(tab==='points') renderAdminPoints();\n  if(tab==='stock') renderAdminStock();\n  if(tab==='ventes') renderAdminSales();\n  if(tab==='caisse') renderAdminCaisse();\n  if"
--- occurrence 13 ---
"bar();\n  const toggle=document.createElement('button'); toggle.className='admin-sidebar-toggle'; toggle.innerHTML='☰'; toggle.onclick=toggleSidebar;\n  const overlay=document.createElement('div'); overlay.className='admin-sidebar-overlay'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement('div'); sidebar.className='admin-sidebar';\n  sidebar.innerHTML = `<ul cl"
--- occurrence 14 ---
'idebar;\n  const overlay=document.createElement(\'div\'); overlay.className=\'admin-sidebar-overlay\'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`)'
--- occurrence 15 ---
'verlay.className=\'admin-sidebar-overlay\'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); d'
--- occurrence 16 ---
'\'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); docu'
--- occurrence 17 ---
'idebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); document.body.appendChild(sid'
--- occurrence 18 ---
'nst sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); document.body.appendChild(sidebar);\n}\nfun'
--- occurrence 19 ---
'v\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); document.body.appendChild(sidebar);\n}\nfunction removeSidebar(){ document.queryS'
--- occurrence 20 ---
"endChild(toggle); document.body.appendChild(overlay); document.body.appendChild(sidebar);\n}\nfunction removeSidebar(){ document.querySelector('.admin-sidebar')?.remove(); document.querySelector('.admin-sidebar-overlay')?.remove(); document.querySelector('.admin-sidebar-toggle')?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector('.admin-sidebar'), o=document.qu"
--- occurrence 21 ---
"ppendChild(sidebar);\n}\nfunction removeSidebar(){ document.querySelector('.admin-sidebar')?.remove(); document.querySelector('.admin-sidebar-overlay')?.remove(); document.querySelector('.admin-sidebar-toggle')?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector('.admin-sidebar'), o=document.querySelector('.admin-sidebar-overlay'); if(!s) return; if(s.classList."
--- occurrence 22 ---
"ument.querySelector('.admin-sidebar')?.remove(); document.querySelector('.admin-sidebar-overlay')?.remove(); document.querySelector('.admin-sidebar-toggle')?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector('.admin-sidebar'), o=document.querySelector('.admin-sidebar-overlay'); if(!s) return; if(s.classList.contains('open')) closeSidebar(); else { s.classList"
--- occurrence 23 ---
"erySelector('.admin-sidebar-overlay')?.remove(); document.querySelector('.admin-sidebar-toggle')?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector('.admin-sidebar'), o=document.querySelector('.admin-sidebar-overlay'); if(!s) return; if(s.classList.contains('open')) closeSidebar(); else { s.classList.add('open'); o.classList.add('open'); } }\nfunction closeSid"
--- occurrence 24 ---
")?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector('.admin-sidebar'), o=document.querySelector('.admin-sidebar-overlay'); if(!s) return; if(s.classList.contains('open')) closeSidebar(); else { s.classList.add('open'); o.classList.add('open'); } }\nfunction closeSidebar(){ document.querySelector('.admin-sidebar')?.classList.remove('open'); document.querySelec"
--- occurrence 25 ---
"st s=document.querySelector('.admin-sidebar'), o=document.querySelector('.admin-sidebar-overlay'); if(!s) return; if(s.classList.contains('open')) closeSidebar(); else { s.classList.add('open'); o.classList.add('open'); } }\nfunction closeSidebar(){ document.querySelector('.admin-sidebar')?.classList.remove('open'); document.querySelector('.admin-sidebar-overlay')?.classList.rem"
--- occurrence 26 ---
"ssList.add('open'); } }\nfunction closeSidebar(){ document.querySelector('.admin-sidebar')?.classList.remove('open'); document.querySelector('.admin-sidebar-overlay')?.classList.remove('open'); }\n\n/* ---------- PRODUITS (manager) ---------- */\nfunction renderAdminProducts(){\n  editingProductId=null; pendingPhotoData=null;\n  document.getElementById('adminBody').innerHTML = `<div "
--- occurrence 27 ---
'tor(\'.admin-sidebar\')?.classList.remove(\'open\'); document.querySelector(\'.admin-sidebar-overlay\')?.classList.remove(\'open\'); }\n\n/* ---------- PRODUITS (manager) ---------- */\nfunction renderAdminProducts(){\n  editingProductId=null; pendingPhotoData=null;\n  document.getElementById(\'adminBody\').innerHTML = `<div class="admin-toolbar"><div class="admin-stat"><b>${PRODUCTS.length}<'

=== files[0] ===
--- occurrence 1 ---
'ById(\'galleryInput\').click(); }\nfunction handlePhotoUpload(e){ const f=e.target.files[0]; if(!f) return; const r=new FileReader(); r.onload=ev=>{ pendingPhotoData=ev.target.result; const pv=document.getElementById(\'photoPreview\'); if(pv) pv.innerHTML=`<img src="${pendingPhotoData}">`; }; r.readAsDataURL(f); }\n\n/* =======================================================\n   ADMIN '

=== reduce( ===
--- occurrence 1 ---
"unction totalStock(productId){ return STOCK.filter(s=>s.product_id===productId).reduce((sum,s)=>sum+s.quantity,0); }\nfunction stockFor(pointId, productId){ const row = STOCK.find(s=>s.point_id===pointId && s.product_id===productId); return row ? row.quantity : 0; }\n\n/* ---------- STOREFRONT ---------- */\nfunction renderMenu(){\n  const gridCafe = document.getElementById('grid-ca"
--- occurrence 2 ---
"(); renderCartBadge(); openDrawer(); }\nfunction renderCartBadge(){ const t=cart.reduce((s,c)=>s+c.qty,0); const b=document.getElementById('cartCount'); if(t>0){b.classList.remove('cart-hidden');b.style.display='flex';b.textContent=t;}else{b.classList.add('cart-hidden');b.style.display='none';} }\nfunction openDrawer(){ checkoutStep='cart'; renderDrawer(); document.getElementById"
--- occurrence 3 ---
"('drawerOverlay').classList.remove('open'); }\nfunction cartTotal(){ return cart.reduce((s,c)=>{const p=PRODUCTS.find(pp=>pp.id===c.id);return s+p.price*c.qty;},0); }\n\nfunction renderDrawer(){\n  const body=document.getElementById('drawerBody'), foot=document.getElementById('drawerFoot');\n  document.getElementById('drawerTitle').textContent = checkoutStep==='cart'?'Votre panier':"
--- occurrence 4 ---
"); if(btn){btn.disabled=true;btn.textContent='Envoi...';}\n  const total = lines.reduce((s,l)=>s+l.sub,0);\n  try{\n    for(const l of lines){ const row=STOCK.find(s=>s.point_id===pointId && s.product_id===l.id); const newQty=row.quantity-l.qty; await sb.from('stock').update({quantity:newQty}).eq('id',row.id); row.quantity=newQty; }\n    const {data:saleRow, error:saleError} = awai"
--- occurrence 5 ---
'vendu = SALES_CACHE.filter(v=>v.status===\'vendu\');\n  const totalRevenue = vendu.reduce((s,v)=>s+v.total,0);\n  const statsEl = document.getElementById(\'salesStats\');\n  if(statsEl){ statsEl.innerHTML = `<div class="admin-stat"><b>${SALES_CACHE.length}</b>commandes</div><div class="admin-stat"><b>${money(totalRevenue)}</b>CA net confirmé</div><div class="admin-stat"><b>${SALES_CAC'
--- occurrence 6 ---
'gin-left:4px;">✕</button></div></div>`; }).join(\'\');\n  const total = caisseCart.reduce((s,c)=>{ const p=PRODUCTS.find(pp=>pp.id===c.id); return s+p.price*c.qty; },0);\n  if(totalEl) totalEl.textContent = money(total);\n}\nfunction setupCaissePaymentButtons(){ document.querySelectorAll(\'#caissePayment button\').forEach(b=>{ b.onclick=()=>{ document.querySelectorAll(\'#caissePayment b'
--- occurrence 7 ---
"me:p.name,qty:c.qty,price:p.price,sub:p.price*c.qty}; });\n  const total = lines.reduce((s,l)=>s+l.sub,0);\n  if(!confirm(`Valider la vente de ${money(total)} ?`)) return;\n  try{\n    for(const l of lines){ const row=STOCK.find(s=>s.point_id===caisseSelectedPoint && s.product_id===l.id); const newQty=row.quantity-l.qty; await sb.from('stock').update({quantity:newQty}).eq('id',row."
--- occurrence 8 ---
" today);\n  if(error){ console.error(error); return; }\n  const totalSales = data.reduce((s,v)=>s+v.total,0);\n  document.getElementById('clotureTotalSales').textContent = money(totalSales);\n  document.getElementById('clotureTxCount').textContent = data.length;\n}\nasync function validateCloture(){\n  const opening = parseInt(document.getElementById('clotureOpening').value)||0;\n  con"
--- occurrence 9 ---
' today);\n  if(error){ console.error(error); return; }\n  const totalSales = data.reduce((s,v)=>s+v.total,0);\n  const expected = totalSales + opening;\n  if(closing !== expected){\n    if(!confirm(`Le montant compté (${money(closing)}) ne correspond pas au montant attendu (${money(expected)} = ventes ${money(totalSales)} + ouverture ${money(opening)}).\\n\\nValider quand même ?`)) re'

=== caisseCart ===
--- occurrence 1 ---
"Point = 'all', SALES_CACHE = [], submitting = false, checkoutStep = 'cart';\nlet caisseCart = [], caisseSelectedPoint = null, CACHED_PENDING_ORDERS = [];\n\nfunction money(n){ return (n||0).toLocaleString('fr-FR') + ' FCFA'; }\nfunction toggleMobileMenu(){ document.getElementById('mobileMenu').classList.add('open'); }\nfunction closeMobileMenu(){ document.getElementById('mobileMenu').classList.remove('open'); }\n\n/* ---------- CHARGEMENT ---------- */\nasync function loadAll(){ await Promise.all([loadProducts(), loadPoints(), loadStock()]); renderMenu(); renderPointCards(); }\nasyn"
--- occurrence 2 ---
'    <div class="admin-toolbar">${pointSelectorHtml}<div class="admin-stat"><b>${caisseCart.length}</b>article(s) au panier</div><button class="btn-ghost" onclick="showClotureModal()">🔒 Clôture de caisse</button></div>\n    <div class="caisse-layout">\n      <div class="caisse-left">\n        <div class="pending-orders-section"><div class="section-title">📋 Commandes en ligne à préparer <span class="count-badge" id="pendingCount">0</span></div><div id="pendingOrdersList"></div></div>\n        <h4 style="margin-bottom:12px;">Produits</h4>\n        <div class="card-grid" id="caisseP'
--- occurrence 3 ---
'    <div class="caisse-cart-header"><h4>Panier</h4><span class="cart-badge" id="caisseCartBadge">${caisseCart.length}</span></div>\n        <div class="caisse-cart-body" id="caisseCartItems"></div>\n        <div class="caisse-cart-footer">\n          <div style="display:flex;justify-content:space-between;font-size:18px;font-weight:700;color:var(--rust);margin:12px 0;"><span>Total</span><span id="caisseTotal">0 FCFA</span></div>\n          <div class="field"><label>Client</label><input type="text" id="caisseClientName" placeholder="Client comptoir"></div>\n          <div class="f'
--- occurrence 4 ---
'sse-cart-header"><h4>Panier</h4><span class="cart-badge" id="caisseCartBadge">${caisseCart.length}</span></div>\n        <div class="caisse-cart-body" id="caisseCartItems"></div>\n        <div class="caisse-cart-footer">\n          <div style="display:flex;justify-content:space-between;font-size:18px;font-weight:700;color:var(--rust);margin:12px 0;"><span>Total</span><span id="caisseTotal">0 FCFA</span></div>\n          <div class="field"><label>Client</label><input type="text" id="caisseClientName" placeholder="Client comptoir"></div>\n          <div class="field"><label>Paieme'
--- occurrence 5 ---
'ge">${caisseCart.length}</span></div>\n        <div class="caisse-cart-body" id="caisseCartItems"></div>\n        <div class="caisse-cart-footer">\n          <div style="display:flex;justify-content:space-between;font-size:18px;font-weight:700;color:var(--rust);margin:12px 0;"><span>Total</span><span id="caisseTotal">0 FCFA</span></div>\n          <div class="field"><label>Client</label><input type="text" id="caisseClientName" placeholder="Client comptoir"></div>\n          <div class="field"><label>Paiement</label><div class="seg" id="caissePayment"><button type="button" data-v'
--- occurrence 6 ---
"oadPendingOrders();\n}\nfunction onCaissePointChange(id){ caisseSelectedPoint=id; caisseCart=[]; renderAdminCaisse(); }\n\n/* commandes en ligne pas encore préparées pour ce point */\nasync function loadPendingOrders(){\n  const {data, error} = await sb.from('sales').select('*, sale_items(*)').eq('point_id', caisseSelectedPoint).eq('channel','en_ligne').eq('fulfilled', false).eq('status','vendu').order('created_at',{ascending:false});\n  if(error){ console.error(error); return; }\n  CACHED_PENDING_ORDERS = data || [];\n  renderPendingOrders();\n}\nfunction renderPendingOrders(){\n  con"
--- occurrence 7 ---
"stock sur ce point.</div>';\n}\nfunction addToCaisse(productId){\n  const existing=caisseCart.find(c=>c.id===productId); const max=stockFor(caisseSelectedPoint,productId);\n  if(existing){ if(existing.qty<max){ existing.qty++; } else { alert('Stock maximum atteint.'); return; } } else { caisseCart.push({id:productId, qty:1}); }\n  renderCaisseCart(); renderCaisseProductGrid();\n}\nfunction removeFromCaisse(productId){ caisseCart = caisseCart.filter(c=>c.id!==productId); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction changeCaisseQty(productId, delta){ const it=caisseCart"
--- occurrence 8 ---
"){ existing.qty++; } else { alert('Stock maximum atteint.'); return; } } else { caisseCart.push({id:productId, qty:1}); }\n  renderCaisseCart(); renderCaisseProductGrid();\n}\nfunction removeFromCaisse(productId){ caisseCart = caisseCart.filter(c=>c.id!==productId); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction changeCaisseQty(productId, delta){ const it=caisseCart.find(c=>c.id===productId); const max=stockFor(caisseSelectedPoint,productId); it.qty=Math.max(1,Math.min(max,it.qty+delta)); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction renderCaisseCart(){\n"
--- occurrence 9 ---
"aisseCart(); renderCaisseProductGrid();\n}\nfunction removeFromCaisse(productId){ caisseCart = caisseCart.filter(c=>c.id!==productId); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction changeCaisseQty(productId, delta){ const it=caisseCart.find(c=>c.id===productId); const max=stockFor(caisseSelectedPoint,productId); it.qty=Math.max(1,Math.min(max,it.qty+delta)); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction renderCaisseCart(){\n  const container=document.getElementById('caisseCartItems'), totalEl=document.getElementById('caisseTotal'), badge=document.getEle"
--- occurrence 10 ---
"renderCaisseProductGrid();\n}\nfunction removeFromCaisse(productId){ caisseCart = caisseCart.filter(c=>c.id!==productId); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction changeCaisseQty(productId, delta){ const it=caisseCart.find(c=>c.id===productId); const max=stockFor(caisseSelectedPoint,productId); it.qty=Math.max(1,Math.min(max,it.qty+delta)); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction renderCaisseCart(){\n  const container=document.getElementById('caisseCartItems'), totalEl=document.getElementById('caisseTotal'), badge=document.getElementById('cai"
--- occurrence 11 ---
"nderCaisseProductGrid(); }\nfunction changeCaisseQty(productId, delta){ const it=caisseCart.find(c=>c.id===productId); const max=stockFor(caisseSelectedPoint,productId); it.qty=Math.max(1,Math.min(max,it.qty+delta)); renderCaisseCart(); renderCaisseProductGrid(); }\nfunction renderCaisseCart(){\n  const container=document.getElementById('caisseCartItems'), totalEl=document.getElementById('caisseTotal'), badge=document.getElementById('caisseCartBadge');\n  if(!container) return;\n  if(badge) badge.textContent = caisseCart.length;\n  if(caisseCart.length===0){ container.innerHTML='"
--- occurrence 12 ---
'id(); }\nfunction renderCaisseCart(){\n  const container=document.getElementById(\'caisseCartItems\'), totalEl=document.getElementById(\'caisseTotal\'), badge=document.getElementById(\'caisseCartBadge\');\n  if(!container) return;\n  if(badge) badge.textContent = caisseCart.length;\n  if(caisseCart.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;padding:20px 0;">Panier vide</p>\'; if(totalEl) totalEl.textContent=\'0 FCFA\'; return; }\n  container.innerHTML = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id);\n    return `<div style="display:flex;gap:10'
--- occurrence 13 ---
' totalEl=document.getElementById(\'caisseTotal\'), badge=document.getElementById(\'caisseCartBadge\');\n  if(!container) return;\n  if(badge) badge.textContent = caisseCart.length;\n  if(caisseCart.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;padding:20px 0;">Panier vide</p>\'; if(totalEl) totalEl.textContent=\'0 FCFA\'; return; }\n  container.innerHTML = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id);\n    return `<div style="display:flex;gap:10px;padding:10px 0;border-bottom:1px solid rgba(110,59,30,0.1);"><div style="flex:1;"><div style="f'
--- occurrence 14 ---
'Id(\'caisseCartBadge\');\n  if(!container) return;\n  if(badge) badge.textContent = caisseCart.length;\n  if(caisseCart.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;padding:20px 0;">Panier vide</p>\'; if(totalEl) totalEl.textContent=\'0 FCFA\'; return; }\n  container.innerHTML = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id);\n    return `<div style="display:flex;gap:10px;padding:10px 0;border-bottom:1px solid rgba(110,59,30,0.1);"><div style="flex:1;"><div style="font-size:13px;font-weight:700;">${p.name}</div><div style="font-size:12px;co'
--- occurrence 15 ---
' if(!container) return;\n  if(badge) badge.textContent = caisseCart.length;\n  if(caisseCart.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;padding:20px 0;">Panier vide</p>\'; if(totalEl) totalEl.textContent=\'0 FCFA\'; return; }\n  container.innerHTML = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id);\n    return `<div style="display:flex;gap:10px;padding:10px 0;border-bottom:1px solid rgba(110,59,30,0.1);"><div style="flex:1;"><div style="font-size:13px;font-weight:700;">${p.name}</div><div style="font-size:12px;color:#8A7660;">${money(p.'
--- occurrence 16 ---
'>\'; if(totalEl) totalEl.textContent=\'0 FCFA\'; return; }\n  container.innerHTML = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id);\n    return `<div style="display:flex;gap:10px;padding:10px 0;border-bottom:1px solid rgba(110,59,30,0.1);"><div style="flex:1;"><div style="font-size:13px;font-weight:700;">${p.name}</div><div style="font-size:12px;color:#8A7660;">${money(p.price)} × ${c.qty} = ${money(p.price*c.qty)}</div></div><div style="display:flex;align-items:center;gap:4px;"><button onclick="changeCaisseQty(\'${c.id}\',-1)" style="width:22px;height:22px;border-rad'
--- occurrence 17 ---
'ze:11px;margin-left:4px;">✕</button></div></div>`; }).join(\'\');\n  const total = caisseCart.reduce((s,c)=>{ const p=PRODUCTS.find(pp=>pp.id===c.id); return s+p.price*c.qty; },0);\n  if(totalEl) totalEl.textContent = money(total);\n}\nfunction setupCaissePaymentButtons(){ document.querySelectorAll(\'#caissePayment button\').forEach(b=>{ b.onclick=()=>{ document.querySelectorAll(\'#caissePayment button\').forEach(x=>x.classList.remove(\'active\')); b.classList.add(\'active\'); }; }); }\n\n/* Vente comptoir : channel=\'caisse\', fulfilled=true, décrémente le stock immédiatement */\nasync funct'
--- occurrence 18 ---
" décrémente le stock immédiatement */\nasync function validerVenteCaisse(){\n  if(caisseCart.length===0){ alert('Panier vide.'); return; }\n  const clientName = document.getElementById('caisseClientName').value.trim() || 'Client comptoir';\n  const payment = document.querySelector('#caissePayment button.active').dataset.val;\n  const lines = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id); return {id:p.id,name:p.name,qty:c.qty,price:p.price,sub:p.price*c.qty}; });\n  const total = lines.reduce((s,l)=>s+l.sub,0);\n  if(!confirm(`Valider la vente de ${money(total)} ?`)) "
--- occurrence 19 ---
"ment.querySelector('#caissePayment button.active').dataset.val;\n  const lines = caisseCart.map(c=>{ const p=PRODUCTS.find(pp=>pp.id===c.id); return {id:p.id,name:p.name,qty:c.qty,price:p.price,sub:p.price*c.qty}; });\n  const total = lines.reduce((s,l)=>s+l.sub,0);\n  if(!confirm(`Valider la vente de ${money(total)} ?`)) return;\n  try{\n    for(const l of lines){ const row=STOCK.find(s=>s.point_id===caisseSelectedPoint && s.product_id===l.id); const newQty=row.quantity-l.qty; await sb.from('stock').update({quantity:newQty}).eq('id',row.id); row.quantity=newQty; }\n    const {da"
--- occurrence 20 ---
'e, subtotal:l.sub}));\n    await sb.from(\'sale_items\').insert(itemsPayload);\n    caisseCart = [];\n    renderCaisseCart(); renderCaisseProductGrid();\n    alert(\'Vente enregistrée !\');\n  } catch(err){ console.error(err); alert("Erreur lors de l\'enregistrement de la vente."); }\n}\n\n/* ---------- CLÔTURE DE CAISSE ---------- */\nfunction showClotureModal(){\n  document.getElementById(\'adminBody\').innerHTML = `<div class="admin-toolbar"><button class="btn-ghost" onclick="renderAdminCaisse()">← Retour à la caisse</button></div>\n    <div style="max-width:500px;margin:20px auto;padding'

=== loadChatMessages ===
--- occurrence 1 ---
'sendChatMessage()">Envoyer</button></div></div>`;\n  loadManagerPhoneIfNeeded(); loadChatMessages(); subscribeToChat();\n}\nasync function loadManagerPhoneIfNeeded(){\n  if(window.__managerPhone) return;\n  const {data} = await sb.from(\'profiles\').select(\'phone\').eq(\'role\',\'manager\').limit(1).single();\n  if(data && data.phone){ window.__managerPhone = data.phone; if(adminProfile.role!==\'manager\' && adminActiveTab===\'assistance\') renderAdminAssistance(); }\n}\nasync function loadChatMessages(){ const {data, error} = await sb.from(\'messages\').select(\'*\').eq(\'point_id\', chatSelectedP'
--- occurrence 2 ---
'\' && adminActiveTab===\'assistance\') renderAdminAssistance(); }\n}\nasync function loadChatMessages(){ const {data, error} = await sb.from(\'messages\').select(\'*\').eq(\'point_id\', chatSelectedPoint).order(\'created_at\',{ascending:true}); if(error){console.error(error);return;} renderChatMessages(data); }\nfunction renderChatMessages(list){\n  const container=document.getElementById(\'chatMessages\'); if(!container) return;\n  if(!list||list.length===0){ container.innerHTML=\'<div class="chat-empty">Aucun message. Démarrez la conversation.</div>\'; return; }\n  container.innerHTML = list.'
--- occurrence 3 ---
"); }\n}\nfunction onChatPointChange(id){ chatSelectedPoint=id; unsubscribeChat(); loadChatMessages(); subscribeToChat(); renderAdminAssistance(); }\nfunction subscribeToChat(){\n  unsubscribeChat();\n  chatChannel = sb.channel('messages-'+chatSelectedPoint).on('postgres_changes', {event:'INSERT', schema:'public', table:'messages', filter:`point_id=eq.${chatSelectedPoint}`}, payload=>{\n    const container=document.getElementById('chatMessages'); if(!container) return;\n    const m=payload.new; const mine=m.sender_role===adminProfile.role; const empty=container.querySelector('.chat"

=== onChatPointChange ===
--- occurrence 1 ---
'r-field"><label>Conversation avec</label><select id="chatPointSelect" onchange="onChatPointChange(this.value)">${POINTS.map(p=>`<option value="${p.id}" ${p.id===chatSelectedPoint?\'selected\':\'\'}>${p.name}</option>`).join(\'\')}</select></div>`\n    : `<div class="admin-stat"><b>Propriétaire</b>assistance TOK\'COS</div>`;\n  const callHref = isManager ? `tel:${(currentPoint?.phone1||\'\').replace(/\\s+/g,\'\')}` : `tel:${(window.__managerPhone||\'77 707 72 70\').replace(/\\s+/g,\'\')}`;\n  con'
--- occurrence 2 ---
'console.error(error); alert("Le message n\'a pas pu être envoyé."); }\n}\nfunction onChatPointChange(id){ chatSelectedPoint=id; unsubscribeChat(); loadChatMessages(); subscribeToChat(); renderAdminAssistance(); }\nfunction subscribeToChat(){\n  unsubscribeChat();\n  chatChannel = sb.channel(\'messages-\'+chatSelectedPoint).on(\'postgres_changes\', {event:\'INSERT\', schema:\'public\', table:\'messages\', filter:`point_id=eq.${chatSelectedPoint}`}, payload=>{\n    const container=document.getE'

=== renderAdminCaisse ===
--- occurrence 1 ---
'nderAdminStock();\n  if(tab===\'ventes\') renderAdminSales();\n  if(tab===\'caisse\') renderAdminCaisse();\n  if(tab===\'utilisateurs\') renderAdminUsers();\n  if(tab===\'assistance\') renderAdminAssistance();\n  closeSidebar();\n}\n\nfunction createSidebar(tabs){\n  removeSidebar();\n  const toggle=document.createElement(\'button\'); toggle.className=\'admin-sidebar-toggle\'; toggle.innerHTML=\'☰\'; toggle.onclick=toggleSidebar;\n  const overlay=document.createElement(\'div\'); overlay.className=\'admin-sidebar-overlay\'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); document.body.appendChild(sidebar);\n}\nfunction removeSidebar(){ document.querySelector(\'.admin-sidebar\')?.remove(); document.querySelector(\'.admin-sidebar-overlay\')?.remove(); document.querySelector(\'.admin-sidebar-toggle\')?.remove(); }\nfunction toggleSidebar(){ const s=document.querySelector(\'.admin-sidebar\'), o=document.querySelector(\'.admin-sidebar-overlay\'); if(!s) return; if(s.classList.contains(\'op'
--- occurrence 2 ---
'- CAISSE (manager: choisit un point / gérant: son point) ---------- */\nfunction renderAdminCaisse(){\n  const isManager = adminProfile.role===\'manager\';\n  if(!isManager){ caisseSelectedPoint = adminProfile.point_id; } else if(!caisseSelectedPoint && POINTS.length){ caisseSelectedPoint = POINTS[0].id; }\n  const pointSelectorHtml = isManager\n    ? `<div class="field admin-toolbar-field"><label>Point de vente</label><select id="caissePointSelect" onchange="onCaissePointChange(this.value)">${POINTS.map(p=>`<option value="${p.id}" ${p.id===caisseSelectedPoint?\'selected\':\'\'}>${p.name}</option>`).join(\'\')}</select></div>`\n    : `<div class="admin-stat"><b>${POINTS.find(p=>p.id===caisseSelectedPoint)?.name||\'\'}</b>votre point de vente</div>`;\n  document.getElementById(\'adminBody\').innerHTML = `\n    <div class="admin-toolbar">${pointSelectorHtml}<div class="admin-stat"><b>${caisseCart.length}</b>article(s) au panier</div><button class="btn-ghost" onclick="showClotureModal()">🔒 Clôture de caisse</button></div>\n    <div class="caisse-layout">\n      <div class="caisse-left">\n        <div class="pending-orders-section"><div class="section-title">📋 Commandes en ligne à préparer <span class="count-badge" id="pendingCount">0</span></div><div id="pendingOrdersList"></div></div>'
--- occurrence 3 ---
's();\n}\nfunction onCaissePointChange(id){ caisseSelectedPoint=id; caisseCart=[]; renderAdminCaisse(); }\n\n/* commandes en ligne pas encore préparées pour ce point */\nasync function loadPendingOrders(){\n  const {data, error} = await sb.from(\'sales\').select(\'*, sale_items(*)\').eq(\'point_id\', caisseSelectedPoint).eq(\'channel\',\'en_ligne\').eq(\'fulfilled\', false).eq(\'status\',\'vendu\').order(\'created_at\',{ascending:false});\n  if(error){ console.error(error); return; }\n  CACHED_PENDING_ORDERS = data || [];\n  renderPendingOrders();\n}\nfunction renderPendingOrders(){\n  const container=document.getElementById(\'pendingOrdersList\'); const badge=document.getElementById(\'pendingCount\');\n  if(!container) return; if(badge) badge.textContent = CACHED_PENDING_ORDERS.length;\n  if(CACHED_PENDING_ORDERS.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;font-size:12px;padding:12px 0;">Aucune commande en attente</p>\'; return; }\n  container.innerHTML = CACHED_PENDING_ORDERS.map(o=>`<div class="pending-order-card"><div class="order-info"><div class="order-client">${o.client_name}</div><div class="order-detail">${o.sale_items.map(i=>i.product_name+\' x\'+i.qty).join(\', \')}</div><div class="order-time">${new Date(o.created_at).toLocaleString(\'fr-FR\')}</div></div><div '
--- occurrence 4 ---
'dy\').innerHTML = `<div class="admin-toolbar"><button class="btn-ghost" onclick="renderAdminCaisse()">← Retour à la caisse</button></div>\n    <div style="max-width:500px;margin:20px auto;padding:24px;background:var(--cream);border-radius:14px;">\n      <h3 style="margin-bottom:20px;color:var(--coffee-black);">🔒 Clôture de caisse — ${POINTS.find(p=>p.id===caisseSelectedPoint)?.name||\'\'}</h3>\n      <div class="field"><label>Montant d\'ouverture (FCFA)</label><input type="number" id="clotureOpening" value="0" min="0"></div>\n      <div class="field"><label>Montant de fermeture compté (FCFA)</label><input type="number" id="clotureClosing" min="0"></div>\n      <div style="background:var(--white);padding:16px;border-radius:10px;margin-bottom:20px;">\n        <div style="font-size:13px;color:#8A7660;margin-bottom:8px;">Résumé du jour (ce point de vente)</div>\n        <div style="display:flex;justify-content:space-between;font-size:14px;margin-bottom:6px;"><span>Total ventes confirmées :</span><span id="clotureTotalSales" style="font-weight:700;">0 FCFA</span></div>\n        <div style="display:flex;justify-content:space-between;font-size:14px;"><span>Nombre de transactions :</span><span id="clotureTxCount" style="font-weight:700;">0</span></div>\n      </div>\n      <button '
--- occurrence 5 ---
'\'enregistrement de la clôture."); return; }\n  alert(\'Clôture enregistrée !\');\n  renderAdminCaisse();\n}\n\n/* ---------- UTILISATEURS (manager uniquement) ---------- */\nfunction renderAdminUsers(){\n  document.getElementById(\'adminBody\').innerHTML = `<div class="admin-toolbar"><div class="admin-stat"><b>Comptes gérants</b></div></div>\n    <p style="font-size:12.5px;color:#8A7660;margin-bottom:16px;">Pour créer un nouveau compte gérant, passez par Supabase Dashboard → Authentication → Users, puis ajoutez une ligne correspondante dans la table <code>profiles</code> (rôle, point de vente). "Retirer l\'accès" ci-dessous supprime le profil applicatif (l\'utilisateur perd immédiatement tout accès) mais ne supprime pas le compte de connexion lui-même.</p>\n    <table class="admin-table"><thead><tr><th>Nom</th><th>Email</th><th>Téléphone</th><th>Point</th><th>Rôle</th><th></th></tr></thead><tbody id="usersTableBody"></tbody></table>`;\n  renderUsersTable();\n}\nasync function renderUsersTable(){\n  const tbody = document.getElementById(\'usersTableBody\'); if(!tbody) return;\n  const {data:profiles, error} = await sb.from(\'profiles\').select(\'*\').order(\'created_at\');\n  if(error){ console.error(error); tbody.innerHTML = \'<tr><td colspan="6" class="admin-empty">Erreur de chargement.</'

=== renderAdminStock ===
--- occurrence 1 ---
'rAdminProducts();\n  if(tab===\'points\') renderAdminPoints();\n  if(tab===\'stock\') renderAdminStock();\n  if(tab===\'ventes\') renderAdminSales();\n  if(tab===\'caisse\') renderAdminCaisse();\n  if(tab===\'utilisateurs\') renderAdminUsers();\n  if(tab===\'assistance\') renderAdminAssistance();\n  closeSidebar();\n}\n\nfunction createSidebar(tabs){\n  removeSidebar();\n  const toggle=document.createElement(\'button\'); toggle.className=\'admin-sidebar-toggle\'; toggle.innerHTML=\'☰\'; toggle.onclick=toggleSidebar;\n  const overlay=document.createElement(\'div\'); overlay.className=\'admin-sidebar-overlay\'; overlay.onclick=closeSidebar;\n  const sidebar=document.createElement(\'div\'); sidebar.className=\'admin-sidebar\';\n  sidebar.innerHTML = `<ul class="admin-sidebar-menu">${tabs.map(([key,label])=>`<li><button data-tab="${key}" onclick="switchAdminTab(\'${key}\')">${label}</button></li>`).join(\'\')}</ul>`;\n  document.body.appendChild(toggle); document.body.appendChild(overlay); document.body.appendChild'
--- occurrence 2 ---
'--- STOCK (manager: tous les points / gérant: son point) ---------- */\nfunction renderAdminStock(){\n  const isManager = adminProfile.role===\'manager\';\n  if(!isManager){ stockSelectedPoint = adminProfile.point_id; } else if(!stockSelectedPoint && POINTS.length){ stockSelectedPoint = POINTS[0].id; }\n  const lowCount = PRODUCTS.filter(p=>stockFor(stockSelectedPoint,p.id)<=5).length;\n  const pointSelectorHtml = isManager\n    ? `<div class="field admin-toolbar-field"><label>Point de vente</label><select id="stockPointSelect" onchange="onStockPointChange(this.value)">${POINTS.map(p=>`<option value="${p.id}" ${p.id===stockSelectedPoint?\'selected\':\'\'}>${p.name}</option>`).join(\'\')}</select></div>`\n    : `<div class="admin-stat"><b>${POINTS.find(p=>p.id===stockSelectedPoint)?.name||\'\'}</b>votre point de vente</div>`;\n  document.getElementById(\'adminBody\').innerHTML = `<div class="admin-toolbar">${pointSelectorHtml}<div class="admin-stat"><b>${lowCount}</b>produit(s) en stock'
--- occurrence 3 ---
'\n  renderStockRows();\n}\nfunction onStockPointChange(id){ stockSelectedPoint=id; renderAdminStock(); }\nfunction renderStockRows(){\n  const tbody=document.getElementById(\'adminStockRows\');\n  if(PRODUCTS.length===0){ tbody.innerHTML=\'<tr><td colspan="4" class="admin-empty">Aucun produit.</td></tr>\'; return; }\n  tbody.innerHTML = PRODUCTS.map(p=>{ const qty=stockFor(stockSelectedPoint,p.id);\n    return `<tr><td>${p.image_url?`<img src="${p.image_url}" class="p-thumb">`:`<div class="stock-thumb-placeholder">${p.icon||\'☕\'}</div>`}</td><td>${p.name}</td><td class="${qty<=5?\'stock-low\':\'\'}">${qty} unités</td><td><div class="stock-adjust"><button onclick="adjustStock(\'${p.id}\',-1)">−</button><input type="number" class="stock-input" value="${qty}" onchange="setStock(\'${p.id}\', this.value)"><button onclick="adjustStock(\'${p.id}\',1)">+</button></div></td></tr>`; }).join(\'\');\n}\nasync function adjustStock(productId, delta){\n  let row=STOCK.find(s=>s.point_id===stockSelectedPoint '

=== product.description ===
--- occurrence 1 ---
'iption</label><textarea id="pfDesc" placeholder="Description courte">${product?(product.description||\'\'):\'\'}</textarea></div>\n    <div class="full admin-form-actions"><button class="btn-primary" onclick="saveProductForm()">${product?\'Enregistrer\':\'Ajouter le produit\'}</button>${p'

=== product.name ===
--- occurrence 1 ---
'd"><label>Nom du produit</label><input type="text" id="pfName" value="${product?product.name.replace(/"/g,\'"\'):\'\'}" placeholder="Ex : Café Touba"></div>\n    <div class="field"><label>Catégorie</label><select id="pfCat"><option value="cafe" ${product&&product.cat===\'cafe\'?\'selecte'

=== point.name ===
--- occurrence 1 ---
'rm">\n    <div class="field"><label>Nom</label><input id="ptName" value="${point?point.name.replace(/"/g,\'"\'):\'\'}" placeholder="Ex : Plateau"></div>\n    <div class="field"><label>Adresse / repère</label><input id="ptAddress" value="${point?point.address.replace(/"/g,\'"\'):\'\'}"></di'

=== point.phone ===
--- occurrence 1 ---
'iv class="field"><label>Téléphone 1</label><input id="ptPhone1" value="${point?(point.phone1||\'\'):\'\'}"></div>\n    <div class="field"><label>Téléphone 2</label><input id="ptPhone2" value="${point?(point.phone2||\'\'):\'\'}"></div>\n    <div class="field full"><label>Recherche Google Ma'
--- occurrence 2 ---
'iv class="field"><label>Téléphone 2</label><input id="ptPhone2" value="${point?(point.phone2||\'\'):\'\'}"></div>\n    <div class="field full"><label>Recherche Google Maps</label><div class="map-search-container"><input id="ptMapQuery" value="${point?(point.map_query||\'\'):\'\'}" placeho'

=== importOrder ===
(none)

=== CACHED_PENDING ===
--- occurrence 1 ---
" false, checkoutStep = 'cart';\nlet caisseCart = [], caisseSelectedPoint = null, CACHED_PENDING_ORDERS = [];\n\nfunction money(n){ return (n||0).toLocaleString('fr-FR') + ' FCFA'; }\nfunction toggleMobileMenu(){ document.getElementById('mobileMenu').classList.add('open'); }\nfunction closeMobileMenu(){ document.getElementById('mobileMenu').classList.remove('open'); }\n\n/* ---------- "
--- occurrence 2 ---
"'created_at',{ascending:false});\n  if(error){ console.error(error); return; }\n  CACHED_PENDING_ORDERS = data || [];\n  renderPendingOrders();\n}\nfunction renderPendingOrders(){\n  const container=document.getElementById('pendingOrdersList'); const badge=document.getElementById('pendingCount');\n  if(!container) return; if(badge) badge.textContent = CACHED_PENDING_ORDERS.length;\n  i"
--- occurrence 3 ---
'entById(\'pendingCount\');\n  if(!container) return; if(badge) badge.textContent = CACHED_PENDING_ORDERS.length;\n  if(CACHED_PENDING_ORDERS.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;font-size:12px;padding:12px 0;">Aucune commande en attente</p>\'; return; }\n  container.innerHTML = CACHED_PENDING_ORDERS.map(o=>`<div class="pending-order-card"><div c'
--- occurrence 4 ---
'ainer) return; if(badge) badge.textContent = CACHED_PENDING_ORDERS.length;\n  if(CACHED_PENDING_ORDERS.length===0){ container.innerHTML=\'<p style="text-align:center;color:#8A7660;font-size:12px;padding:12px 0;">Aucune commande en attente</p>\'; return; }\n  container.innerHTML = CACHED_PENDING_ORDERS.map(o=>`<div class="pending-order-card"><div class="order-info"><div class="order'
--- occurrence 5 ---
'ing:12px 0;">Aucune commande en attente</p>\'; return; }\n  container.innerHTML = CACHED_PENDING_ORDERS.map(o=>`<div class="pending-order-card"><div class="order-info"><div class="order-client">${o.client_name}</div><div class="order-detail">${o.sale_items.map(i=>i.product_name+\' x\'+i.qty).join(\', \')}</div><div class="order-time">${new Date(o.created_at).toLocaleString(\'fr-FR\')}<'
--- occurrence 6 ---
"mmande en ligne) */\nasync function markOrderFulfilled(orderId){\n  const order = CACHED_PENDING_ORDERS.find(o=>o.id===orderId); if(!order) return;\n  if(!confirm(`Confirmer la remise de la commande de ${order.client_name} (${money(order.total)}) ?`)) return;\n  const {error} = await sb.from('sales').update({fulfilled:true}).eq('id', orderId);\n  if(error){ console.error(error); ale"
