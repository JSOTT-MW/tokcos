=== renderAdminShell ===
"renderAdminShell(){\n  if(!adminProfile){ renderAdminLogin(); return; }\n  const isManager = adminProfile.role==='manager'"

=== m.body context ===
'iv class="chat-bubble ${mine?\'mine\':\'theirs\'}">${escapeHtml(m.body)}<div class="chat-meta">${escapeHtml(m.sender_name || \'\')} · ${new Date(m.created_at).toLocal'

=== insertAdjacentHTML ===
'insertAdjacentHTML(\'beforeend\', `<div class="chat-bubble ${mine?\'mine\':\'theirs\'}">${escapeHtml(m.body)}<div class="chat-meta">${escapeHtml(m.sender_name || \'\')} · ${new Date(m.created_at).toLocaleStri'

=== order-client ===
'order-client">${escapeHtml(order.client_name)}</div><div class="order-detail">${order.sale_items.map(i=>escapeHtml(i.product_name)).join(\', \')}</div><div class="order-time">${new Date(order.created_at'

=== Assistance ===
'\'\')}</select></div>` : \'\'; body.innerHTML = `<div class="admin-toolbar">${chatPointSelectorHtml}<div class="admin-stat"><b>Assistance</b></div></div><div class="chat-wrap"><div class="chat-messages" id="chatMessages"><div class="chat-empty">Chargement...</div></div><div class="chat-input-row"><input type="text" id="cha'

=== chat-wrap ===
' .status-btn{font-size:9px;padding:3px 6px;}\n    .chat-wrap{flex-direction:column;gap:12px;}\n    .chat-messages{max-height:400px;}\n    .chat-input-row{flex-direction:column;gap:8px;}\n    .chat-input-row input{font-size:14px;padding:12px;}\n    .chat-input-row button{padding:12px;font-size:14px;}\n    .chat-bubble{font-size:13px;padding:8px 12px;}\n   '